version: 2.1
orbs:
  docker: circleci/docker@1.5.0
                     
workflows:
  default:
    jobs:
     - docker/hadolint:
        dockerfiles: ./Dockerfile
     - docker/publish:
        requires: [docker/hadolint]
        image: krajeevs/krajeevs_v1
        tag: latest
